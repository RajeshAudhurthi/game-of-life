apiVersion: apps/v1
kind: Deployment
metadata:
  name: gol-deployment
  labels:
    app: gol
spec:
  replicas: 3
  selector:
    matchLabels:
      app: gol
  template:
    metadata:
      labels:
        app: gol
    spec:
      containers:
      - name: gol
        image: raajesh404/gol1:latest5
        ports:
        - containerPort: 8080